# PRD: 건설사업관리 직접경비 정산 플랫폼

- **서비스 표기명**: CM 직접경비 정산 플랫폼 (2026-07-30 확정 — 로그인 화면·사이드바·정산서 엑셀 작성자명 등 전 화면 통일)
- **버전**: v1.0 (MVP)
- **작성일**: 2026-06-30
- **작성자**: 정성호 / 선엔지니어링 건설사업관리본부
- **적용 기준**: 건설엔지니어링 대가 등에 관한 기준 (직접경비 정산)
- **상태**: 검토 중 (Draft)

---

## 1. 개요 (Overview)

### 1.1 배경 및 문제 정의

건설사업관리(CM) 용역사는 발주처에 기성을 청구할 때 현장 직접경비(숙식비, 교통비, 현장운영비 등)를 정산하여 증빙과 함께 제출하여야 한다. 현재 선엔지니어링은 다음과 같은 아날로그 방식에 의존하고 있다.

- 현장 직원이 엑셀로 비용 내역을 수기 작성
- 종이 영수증 또는 이미지 파일을 별도 수집하여 본사에 우편·이메일 제출
- 본사 담당자가 수작업으로 규정 적합성 검토(공무원 여비규정, 건설엔지니어링 대가 기준 등)
- 반려 시 현장 재제출 → 기성 청구 지연 반복 발생

이로 인해 발생하는 핵심 문제:

- **증빙 누락**: 항목별 필수 증빙자료(세금계산서, 출근부 등) 미제출로 발주처 반려
- **규정 위반**: 식대 2.5만원 한도, 복리후생비 5만원/인·월 한도 등 공무원 여비규정 초과 입력
- **정산 지연**: 기성 청구 마감 전 수작업 검토에 2~3일 소요
- **데이터 분산**: 프로젝트별 비용 데이터가 개인 PC에 산재하여 이력 추적 불가

### 1.2 제품 목적

| 목표      | 내용                 | KPI                      |
| ------- | ------------------ | ------------------------ |
| 업무 효율화  | 정산서 작성 및 검토 시간 단축  | 정산서 작성 시간 80% 단축 (기존 대비) |
| 오류 제거   | 시스템이 규정 준수를 자동 검증  | 규정 위반으로 인한 반려율 0건 목표     |
| 데이터 자산화 | 프로젝트별 비용 데이터 누적 관리 | 전사 11개 이상 현장 데이터 통합 조회   |

### 1.3 제품 범위

- **대상 조직**: 선엔지니어링 건설사업관리본부 전 현장 (11개 이상)
- **1단계(MVP)**: 핵심 직접경비 항목 입력·검증·정산서 출력
- **2단계**: 기성 청구 패키지 생성, 발주처 제출용 공식 출력물 완성
- **제외 범위**: 발주처 시스템 직접 연동, 급여·노무비 정산, ERP 연동, 회계 시스템 연계 (추후 검토), **배치표 기반 직접경비(계상금액) 자동 산출** — 계상금액은 계약 내역서 값을 사용자가 직접 입력한다 (2026-07-30 확정)

---

## 2. 사용자 및 역할 (Users & Roles)

| 역할        | 주요 업무                    | 주요 권한                     | 비고                         |
| --------- | ------------------------ | ------------------------- | -------------------------- |
| 현장 직원     | 비용 입력, 영수증 업로드, 월별 내역 관리 | 소속 현장 데이터 입력·조회           | IT 숙련도 낮을 수 있음 → UI 단순화 필수 |
| 본사 정산 담당자 | 전 현장 정산 내역 최종 확인, 출력     | 전 현장 조회, 엑셀·PDF 출력, 설정 변경 | 정성호 등 CM 본부 담당자            |
| 시스템 관리자   | 현장·사용자 등록, 정산 기준 파라미터 관리 | 전체 시스템 관리                 | 사내 IT 또는 담당자 겸임            |

> 본 시스템은 **현장 직원 → 본사 정산 담당자** 2단계 구조로 운영되며, 별도 현장 PM/책임기술인 승인 단계 및 발주처 직접 접근 기능은 포함하지 않는다.

---

## 3. 직접경비 정산 기준 (Settlement Rules)

「건설엔지니어링 대가 등에 관한 기준」상 직접경비 정산 기준표를 시스템 로직으로 구현한다.

### 3.1 비목별 정산 기준표

| 항목     | 세부항목          | 정산내용                | 적용기준 (한도)                   | 필수 증빙자료                                                 |
| ------ | ------------- | ------------------- | --------------------------- | ------------------------------------------------------- |
| 현장주재비  | 숙식비(임대비)      | 월세, 전세              | 실비 적용                       | 월세·전세 계약서, 입금 확인증, 산출내역(전월세 전환율, KB국민은행 기준금리)           |
| 현장주재비  | 숙식비(관리비)      | 전기, 수도 등            | 실비 적용                       | 관리비 계약서, 관리비 입금 확인증                                     |
| 현장주재비  | 숙식비(식대)       | 식대                  | **2.5만원/1인·1일 한도**          | 출근부 (공무원여비규정 적용)                                        |
| 현장주재비  | 교통비(출퇴근)      | 버스, 철도, 자차 등        | 공무원 여비규정 적용 / 상주기술인에 한해     | 출근부, 시내버스 대중교통×2, 월4회 현장↔주거지 교통비 승차권, 자차 이용 시 통행료·연비계산서 |
| 현장주재비  | 사무용품비         | 사무기기 임대료, 소모품비      | 실비 적용                       | 용품구입 영수증, 세금계산서                                         |
| 현장주재비  | 안전용품비         | 개인 안전장구             | 실비 적용                       | 용품구입 영수증, 세금계산서                                         |
| 현장주재비  | 통신비           | 인터넷, 공용전화 등(설치비 포함) | 실비 적용 (**개인 휴대폰 이용금액 불인정**) | 사용 영수증, 납입 확인증                                          |
| 현장주재비  | 복리후생비         | 음료, 간식, 회의비용, 회식 등  | **5만원/1인·1월 한도**            | 비용 산출내역서 및 확인증                                          |
| 현장주재비  | 사무실비          | 사무실 임대료, 공과금        | 실비 적용                       | 입금 확인증 및 공과금 영수증                                        |
| 차량운행비  | 임대비, 유류비      | 임대비, 유류비            | 실비 적용                       | 운임·통행료·주차료 등 영수증                                        |
| 출장비    | 숙박비/일비/식비/교통비 | 숙박비, 일비, 식비, 교통비    | 공무원 여비규정 적용                 | 영수증 (일비는 별도 증빙 불필요)                                     |
| 현지사무원비 | 현지 사무원 급여     | 급여(입금증 등)           | 실비 적용 / 근로기준법 준수            | 급여지출내역, 수령확인증                                           |
| 도서인쇄비  | 인쇄, 제본        | 인쇄, 제본              | 실비 적용 (과업지시서 참고)            | 인쇄·제본 등 비용 영수증                                          |

### 3.2 기타사항 (시스템 적용 원칙)

- 항목별 비용은 직접경비 내에서 항목 간 이동(변경)이 가능하며, 발주청은 공사 및 지역 특성을 고려하여 직접경비를 증·감할 수 있다. (국토교통부 고시 제2023-580호 [별표2])
- 발주청은 정산 내용에 맞게 사용된 금액에 대해 별도 증빙자료를 검토하여 인정·정산할 수 있다.
- 발주청은 직접경비의 사용금액이 항목별 목적에 부합하지 아니할 경우 해당 금액을 불인정하여야 한다.
- 시스템은 한도 초과 입력 시 즉시 경고를 표시하고, 초과분은 자동으로 표시 분리하여 정산 대상에서 제외한다.

### 3.3 기성 차수별 계상금액·청구 모델 (실제 차수별 정산서 기준)

실제 발주처 제출 정산서(예: 청주 도매시장 1~5회 기성)에서 확인된 정산 구조를 시스템 모델로 채택한다.

- **계상금액**: 계약 내역서에서 직접경비는 항목별로 계상된다 (주재비=상주인건비×10%, 출장비=기술지원인건비×5%, 도서인쇄비=별도 금액 — 이 산출은 배치표 기반으로 계약 단계에서 이뤄지며, **시스템은 산출하지 않고 그 결과 금액을 직접 입력받는다**). 시스템은 현장별·항목별 계상금액(`site_expense_budgets`)을 관리하고, 총액(`direct_expense_budget`)은 항목 합계로 동기화한다. 입력 위치: ①관리자 현장 등록/수정 폼 ②현장직원 기성회차 정산 화면 입력 카드 ③회차 생성 시 금회 계상액(선택).
- **회차별 정산**: 기성 차수마다 정산서 2번 표 [항목별 계약금액 | 전회누계금액 | 금회기성금액 | 잔액]을 작성한다. 누계·잔액은 **청구(기성)액 기준**으로 관리한다.
- **청구 캡**: 발주청 지급액 = min(증빙된 사용액, 계상총액 잔액). 최종 회차에서 잔액을 초과한 사용분은 청구 불가(미지급)이며, 시스템이 확정 시 자동 계산·경고한다.
- **항목 간 이동**: 항목별 계상 초과라도 직접경비 총액 내이면 타 항목 잔액에서 흡수 가능(고시 별표2). 시스템은 항목 초과를 "불인정"이 아닌 "총액 내 흡수 가능" 안내로 처리한다.
- **금회 계상액(선택)**: 회차 생성 시 해당 회차 산출내역서의 직접경비 계상액을 입력하면, 사용액과 비교해 부족(삭감 위험)·초과를 경고한다.
- **사용액 vs 청구액 구분**: 회차 확정 시 사용액(`current_round_amount`)과 청구액(`claim_amount`)을 분리 스냅샷하고, 항목별 내역은 `settlement_round_items`에 보존한다 (감사 추적).

---

## 4. 기능 요구사항 — 1단계 MVP

### 4.1 사용자 인증 및 현장 관리

| ID   | 요구사항                                               | 우선순위 | 비고                     |
| ---- | -------------------------------------------------- | ---- | ---------------------- |
| F-01 | 이메일+비밀번호 기반 로그인 / 역할별 접근 권한 분리                     | 필수   | 현장직원·본사담당자·관리자 3개 역할   |
| F-02 | 현장(프로젝트) 등록: 현장명, 발주처, 계약기간, 계약금액, 직접경비 예산 입력      | 필수   | 관리자 전용 기능              |
| F-03 | 현장별 정산 기준 파라미터 설정 (식대 한도, 복리후생비 한도, 공무원 여비규정 등급 등) | 필수   | 계약 조건에 따라 현장별 상이할 수 있음 |
| F-04 | 사용자 등록 및 현장 배정 (1인이 복수 현장 소속 가능)                   | 필수   | ✅ 구현완료(2026-07-23): 현장 배정 시 상주기술인/기술지원 구분(staff_type) 추가, 인원별 주재비 화면이 이 구분으로 분리됨 |

### 4.2 직접경비 입력 (현장 직원)

| ID   | 요구사항                                                  | 우선순위 | 비고                                                                              |
| ---- | ----------------------------------------------------- | ---- | ------------------------------------------------------------------------------- |
| F-05 | 항목별 비용 입력 폼 제공 (현장주재비·차량운행비·출장비·현지사무원비·도서인쇄비)         | 필수   | 비목 → 세부항목 순 계층 선택 UI                                                            |
| F-06 | 영수증 이미지 업로드 (JPG, PNG, PDF 지원, 파일당 최대 10MB)           | 필수   | 모바일 카메라 촬영 업로드 지원                                                               |
| F-07 | 입력 시 필수 증빙자료 안내 자동 표시 (비목 선택 즉시)                      | 필수   | 예: 식대 선택 → '출근부 필요' 표시                                                          |
| F-08 | 식대 2.5만원/인·일, 복리후생비 5만원/인·월 등 한도 초과 시 실시간 경고 및 초과분 표시 | 필수   | 저장 가능하되, 초과분은 '불인정' 표시 분리 · ✅ 복리후생비 한도체크 구현됨, 식대 한도는 현장별 파라미터 연동 완료(2026-07-23) |
| F-09 | 임시저장 기능: 작성 중 데이터 유실 방지                               | 필수   |                                                                                 |
| F-10 | 월별 입력 현황 대시보드: 항목별 누계, 잔여 예산 표시                       | 필수   |                                                                                 |
| F-11 | 출근부 연동: 식대·교통비 계산을 위한 현장별 인원 출근일수 입력                  | 권장   | ✅ 1단계 구현(2026-08-03): 현장 작성·서명 출근부를 상주/기술지원 구분 첨부(스캔) + 인원별 일수(상주)·방문일자(기술지원) 전기. 일수는 주재비 폼(식대·출퇴근교통비)에 자동 주입. 출장비 방문일↔출근부 대조 검증은 2단계 |

### 4.3 자동 규정 검증 엔진

| ID   | 요구사항                                                 | 우선순위 | 비고                                                                                        |
| ---- | ---------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------- |
| F-12 | 비목별 한도 검증: 입력값이 설정 파라미터를 초과하면 경고 및 상태 표시 (정상/경고/불인정) | 필수   |                                                                                           |
| F-13 | 필수 증빙자료 누락 체크: 항목별 필수 증빙 미첨부 시 승인 불가 처리              | 필수   | 미첨부 항목 목록 표시                                                                              |
| F-14 | 개인 휴대폰 통신비 불인정 규칙 자동 적용 (통신비 입력 시 안내 표시)             | 필수   | ✅ 구현완료(2026-07-23): 통신비 입력 시 확인 체크 전까지 저장 차단                                              |
| F-15 | 자차 교통비 입력 시 통행료·연비계산서 첨부 필수 안내 자동 노출                 | 필수   | ✅ 구현완료·활성화(2026-08-04): 카카오 API로 전환 — 카카오 로컬(주소→좌표) + 카카오모빌리티 자동차 길찾기(거리·통행료, 일 1만건 무료)로 조회 후 「공무원보수 등의 업무지침」 공식(거리×유가÷연비) 자동계산. REST API 키 발급·카카오맵 활성화 완료, 실호출 검증(서울 용산↔청주 133.34km·통행료 4,800원) |
| F-16 | 현장별 파라미터 기반 동적 검증: 현장마다 다른 한도 기준 적용 가능               | 필수   | 공무원 여비규정 등급 현장별 상이 · ✅ 식대 한도·출퇴근교통비 적용여부 현장별 파라미터 연동 완료(2026-07-23), 여비규정 등급→금액 매핑은 추후 과제 |

### 4.4 검토·승인 워크플로

| ID   | 요구사항                                            | 우선순위 | 비고                                                                                                                       |
| ---- | ----------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------ |
| F-17 | 현장 직원 → 본사 담당자 직접 제출 및 승인/반려 기능 (반려 시 사유 입력 필수) | 필수   | 중간 PM 검토 단계 없음 · ✅ 구현완료(2026-07-30): `/hq/review` 제출 검토 화면 — 개별·현장별 일괄 승인, 반려 사유 필수(현장 직원에게 표시), 반려 건은 회차 편입 제외·삭제 후 재입력 |
| F-18 | 본사 담당자 최종 확인 후 정산 확정 처리 (확정 시 수정 잠금)            | 필수   | ✅ 구현완료(2026-07-30): 개별 승인(approved) + 기성회차 확정 시 편입·잠금. 확정 RPC v3가 미검토 제출건과 개별 승인건을 함께 편입(반려 건 제외)                          |
| F-19 | 제출·승인·반려 단계별 이메일 알림 발송                          | 권장   | MVP: 사내 이메일 기반                                                                                                           |
| F-20 | 전체 현장 정산 현황 모니터링 화면 (본사 담당자 전용)                 | 필수   | 현장별 상태: 입력중/검토중/확정/출력완료 · ✅ 구현완료(2026-07-23): `/hq/overview` — 예산·확정사용액·잔액·검토대기 일람 + 현장별 드릴다운. 검토대기에서 제출 검토 화면(F-17)으로 연결(2026-07-30) |

### 4.5 정산서 출력 (엑셀·PDF)

| ID   | 요구사항                                               | 우선순위 | 비고                 |
| ---- | -------------------------------------------------- | ---- | ------------------ |
| F-21 | 월별·기성회차별 직접경비 정산서 엑셀(xlsx) 출력: 비목·세부항목·금액·증빙 목록 포함 | 필수   | 발주처 제출용 양식 기준 · ✅ 구현완료(2026-07-29): 총괄표+세부 사용내역 시트 전체, 기성회차 화면 다운로드 |
| F-22 | 동일 내용 PDF 출력 (인쇄 즉시 제출 가능한 포맷)                     | 필수   | ✅ 구현완료(2026-08-03): pdfmake + Noto Sans KR 임베딩, 엑셀과 동일 서식(총괄+세부내역 전체), `/api/reports/pdf` + 기성회차 화면(현장·본사) PDF 다운로드 버튼 |
| F-23 | 불인정 항목은 출력물에서 별도 음영 처리하고 합계에서 제외                   | 필수   | ✅ 구현완료(2026-07-29): 인정금액(사용금액−불인정분) 기준 집계로 합계 제외 반영 |
| F-24 | 업로드된 영수증 이미지를 비목별로 정렬하여 PDF 증빙 파일 묶음 출력            | 권장   | 2단계 우선 검토          |
| F-25 | 출력 이력 관리: 출력 일시·출력자 기록 보존                          | 권장   | 감사 추적 목적           |

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

### 5.1 UI/UX

- 현장 직원의 IT 숙련도가 낮음을 전제로 3-클릭 이내 주요 기능 접근 구현
- 모바일 반응형 웹 지원 (현장에서 스마트폰으로 영수증 즉시 업로드 가능)
- 비목 선택 후 필수 증빙 안내가 폼 내에 즉각 표시되어 '몰라서 누락' 방지
- 오류 메시지: 무엇이 왜 잘못됐는지, 어떻게 수정하면 되는지 구체적으로 안내

### 5.2 성능

- 페이지 로딩: 3초 이내 (일반 LTE 환경 기준)
- 영수증 업로드: 10MB 이하 파일 업로드 5초 이내 완료
- 정산서 출력(엑셀·PDF): 30초 이내 생성
- 동시 사용자 50명 이상 처리 가능

### 5.3 보안

- HTTPS 통신 필수 (TLS 1.2 이상)
- 역할 기반 접근 제어(RBAC): 타 현장 데이터 접근 차단
- 영수증 파일 저장소 접근은 서버 서명 URL(Pre-signed URL) 방식 적용
- 금융 정보·계약 관련 민감 데이터 암호화 저장 (AES-256 이상)
- 세션 타임아웃: 비활성 30분 후 자동 로그아웃

### 5.4 규정 유연성

- 현장별 정산 기준 파라미터(식대 한도, 복리후생비 한도, 여비규정 등급 등)를 관리자가 UI에서 직접 변경 가능
- 법령 개정 시 파라미터 업데이트만으로 시스템 전체 적용 가능한 구조

### 5.5 데이터 보존

- 정산 데이터 및 영수증: 최소 5년 보존 (「국가계약법」 계약 서류 보존 기간 기준)
- 삭제는 논리 삭제 방식으로, 데이터 복구 가능

---

## 6. 주요 화면 구성 (MVP 기준)

| 화면       | 주요 구성 요소                                            | 접근 권한  |
| -------- | --------------------------------------------------- | ------ |
| 로그인      | 이메일·비밀번호, 비밀번호 찾기                                   | 전체     |
| 현장 대시보드  | 이번 달 비목별 사용 현황, 잔여 예산, 제출 상태, 알림                    | 현장 직원  |
| 비용 입력 폼  | 비목 선택 → 세부항목 → 금액·날짜·메모 입력 → 영수증 업로드 → 필수증빙 안내 → 저장 | 현장 직원  |
| 월별 내역 목록 | 입력된 비용 목록, 상태 필터, 수정·삭제, 합계 요약                      | 현장 직원  |
| 본사 전체 현황 | 전 현장 정산 상태 일람, 현장별 드릴다운, 승인·반려, 출력 버튼               | 본사 담당자 |
| 정산서 출력   | 기간 선택, 기성회차, 미리보기, 엑셀·PDF 다운로드                      | 본사 담당자 |
| 시스템 설정   | 현장 등록·수정, 사용자 관리, 정산 기준 파라미터 설정                     | 관리자    |

---

## 7. 개발 단계 로드맵

| 단계       | 기간 (예상)        | 주요 기능                                                                 | 목표 상태             |
| -------- | -------------- | --------------------------------------------------------------------- | ----------------- |
| 1단계 MVP  | 착수 후 약 3개월     | 로그인·권한, 현장 등록, 비용 입력 폼 (5대 비목), 영수증 업로드, 규정 검증 엔진, 승인 워크플로, 엑셀·PDF 출력 | 파일럿 현장 1~2개 적용·검증 |
| 2단계      | MVP 완료 후 약 2개월 | 영수증 PDF 묶음 출력, 출근부 연동, 기성 패키지 생성, 전사 현장 확대, 대시보드 고도화                  | 전사 11개 이상 현장 배포   |
| 3단계 (검토) | 추후 결정          | ERP 연동, AI 기반 영수증 자동 인식(OCR), 예산 분석 리포트                               | 고도화 기능 선택 적용      |

---

## 8. 미결 사항 및 추가 확인 필요 항목

| No. | 미결 사항             | 결정 필요 내용                                           | 담당        |
| --- | ----------------- | -------------------------------------------------- | --------- |
| 1   | 발주처별 정산 양식 차이     | 발주처마다 요구하는 엑셀 양식이 상이할 경우, 양식 템플릿 커스터마이징 기능 포함 여부   | 본사 담당자    |
| 2   | 공무원 여비규정 등급 적용 기준 | 현장별로 기술인 직급에 따라 여비 등급이 다름. 직급 정보를 시스템에서 어떻게 관리할지   | 본사 담당자    |
| 3   | 출근부 양식 표준화        | 식대·교통비 산정 기준이 되는 출근부를 시스템 내 입력할지, 별도 첨부로 처리할지      | 본사 담당자    |
| 4   | 항목 간 예산 이동 승인 절차  | 직접경비 내 항목 간 이동(예: 복리후생비 → 사무용품비) 시 발주청 사전 승인 여부 확인 | 본사·발주처 협의 |
| 5   | 인프라 방식 결정         | 클라우드(SaaS) 방식 vs 사내 서버 구축 vs 외주 개발사 여부             | 경영진       |

---

## 8.1 구현 현황 로그

| 날짜         | 항목    | 내용                                                                                                        |
| ---------- | ----- | --------------------------------------------------------------------------------------------------------- |
| 2026-08-11 | F-16  | **상주기술인 거주 형태 구분 (숙소 사용 / 자가 출퇴근)** — 예본 「1-1 상주기술인 숙소비 사용내역」은 상주 13명 중 8명만 대상(강희철·이병필·신우상·정상윤·지창호는 자가출퇴근)인데, 화면에서 구분이 안 되고 출퇴근자에게 숙소비를 입력해도 경고가 없었다. 기존 `commuteMode`(숙박형/출퇴근형)가 교통비 승수 계산에만 쓰이던 것을 1급 개념으로 승격. ①명부에 `residence_type`(`lodging`/`commute`, migration 020) 추가 — 사람의 속성이므로 정산 인원의 단일 원천인 명부에 두고, 출근부 화면 상주 표·인원 추가 폼에서 지정(`updateSiteStaffResidence`) ②주재비 폼 성명 열에 `🏠 숙소`/`🚗 출퇴근` 토글(명부값 초기화, 회차 중 이사 시 그 회차만 덮어쓰기), 자가 출퇴근이면 숙소임대비·관리비 칸을 "해당 없음"으로 잠그고 패널 버튼 숨김 — 교통비 칸의 유형 select는 제거하고 승수 기준만 표시(결정 지점 단일화) ③기존 데이터가 어긋나면 "저장하면 0으로 정리됩니다" 경고 ④서버가 클라이언트 값을 신뢰하지 않고 `daily_commute`면 숙소비를 0으로 확정·건별 내역 제거, 단건 저장 경로도 거부. 변환 규칙 테스트 4종 |
| 2026-08-11 | F-16  | **주재비 영수증 증분 저장 (첨부 즉시 업로드 + 비목 단위 확정)** — 영수증 한 묶음 올릴 때마다 저장해 나가는 흐름. 종전에는 첨부가 브라우저 메모리에만 있고 「임시저장」 한 번으로 전체를 올려야 했다. ①**선행 결함 2건 수정**: 저장된 draft(금액·건별 내역·첨부)를 폼에 복원하지 않아 재진입 후 빈 폼으로 저장하면 기존 draft가 삭제되던 문제 — `_shared.tsx`가 `expense_items`·`commute_calcs`까지 조인해 복원. `receipt_urls`를 새 파일로 **덮어써** 이전 첨부가 유실되던 문제 — 화면에 남은 URL(`kept::`)과 신규 업로드분을 병합하도록 변경 ②`attachStaffCostReceipt`/`detachStaffCostReceipt` — 드롭 즉시 storage 업로드 + draft 행에 URL 부착(금액 불변), 패널이 링크·비목 칩·✕로 표시. 금액 확정 전 첨부만 담을 행이 필요해 `expenses.amount` CHECK를 `amount > 0 OR status='draft'`로 완화(migration 019, 제출 이후 단계는 양수 유지) ③`saveStaffCostItem` — 숙소임대비·관리비를 비목 1건만 upsert(`identity::subcategory::period_start` 동일 키로 전체 저장과 충돌 없음), 패널 하단 「관리비 저장」·「숙소임대비 저장」 + 저장됨 표시 ④금액 0으로 남기는 행은 `amount_gross`·`vat_mode`·`calc_detail`·자식 레코드까지 비워 유령 금액 방지 |
| 2026-08-11 | UI    | **주재비 표 패널 잘림 수정** — 행별 패널(영수증·관리비 내역·자차 산출·숙소 계약)이 가로 스크롤되는 표 안에 있어 표를 오른쪽으로 밀면 내용이 왼쪽으로 잘렸다. 패널을 `sticky left-0`으로 붙이고 폭을 스크롤 영역의 보이는 폭(`--panel-w`, ResizeObserver로 동기화)으로 고정 |
| 2026-08-11 | 테스트환경 | **명부 이름 정정·테스트 출근부 재작성** — 상주 건축 감리원 정상운→성혁기로 환원(예본 명단과 일치, `site_staff_members.name`과 `expenses.target_user_name` 동시 갱신 — 이름이 정산 데이터의 식별자라 한쪽만 바꾸면 draft가 고아가 됨). 배포용 테스트 출근부를 현재 5회차 기준으로 재작성(`Desktop\웹사이트(정산서)\테스트영수증\테스트_출근부_상주_2026-04~08.pdf` 6p + PNG): 강희철(책임)·성혁기(건축), 월별 22/19/22/23/20일 합계 106일, 파서가 읽는 총괄표 형식. 실제 `parseResidentDays`로 검증하는 테스트 2종 추가(파일 없으면 skip). 기존 `테스트_출근부_2026년7월`(정성호·한강cm)은 보존 |
| 2026-08-10 | F-16  | **주재비 영수증 금액 자동 인식** — 인원 행에 PDF 첨부 시 즉시 금액 추출·자동 입력(`src/lib/receipts/parseReceipt.ts` + `parseReceiptAmounts` 액션, 출근부 인식과 동일한 unpdf 기반): ①숙소임대비 = 이체확인증의 "이체금액" 합산(월별 페이지 묶음 1부 대응) ②관리비 = 전기·가스 건별 내역(입금일자·구분·금액) 자동 추가 — 단월 양식·기간 통합 표 모두 인식, 중복 방지, VAT 제외 적용금액 계산은 기존 로직 연계. 인식값은 제안(확인 후 임시저장). 실PDF 픽스처 테스트 4종. 첨부 개선: 파일명 키워드로 비목 자동 분류(숙소비→숙소임대비 등), 인원당 첨부 한도 5개→기성기간 개월수 비례(최소 30). 출근부 인식 안내가 성공인데 경고로 뜨던 React 상태 타이밍 버그 수정 |
| 2026-08-10 | F-15  | **자차 산출 개선 2종** — ①현장주소를 패널에서 직접 입력·저장 가능(기존 읽기 전용): 소속 현장직원 권한 확인 후 `sites.address` 갱신(`updateSiteAddress`), 한 번 저장하면 전 인원·다음 방문에 자동 입력 ②**고속도로 우선 경로 옵션**(기본 ON): 카카오 길찾기 priority=TIME으로 고속도로 기준 거리·통행료 산출(추천 경로가 무료도로면 통행료 0원 나오는 문제 대응). 실호출 검증: 서울→청주 추천 4,800원 vs 시간우선 6,200원(편도). 미결: 기성기간 5개월 유가 변동 반영(월별 산출 그리드) — B안 제안 상태 |
| 2026-08-10 | 테스트환경 | **재테스트 데이터 정비** — 상주 건축 감리원 성혁기→정상운 교체(출퇴근형 교통비 검증용, 예본 보존 명단은 유지), 샌드박스 출근부 기록 초기화. 테스트 증빙을 `Desktop\테스트영수증_신규\`에 기성기간 1부 형식으로 재구성(출근부 상주·기술지원, 숙소임대비 이체확인증 월별 5장 묶음, 관리비 납입확인서 월별 5장 묶음 — 전기 539,770+가스 302,400=842,170원) |
| 2026-08-10 | F-11  | **출근부 기성회차 단위 개편** — 연월 조회를 없애고 회차 중심으로 재구성. ①"기성기간" 패널(연·월·일 날짜 입력): 진행 중 회차 기간 표시·수정, 진행 중 회차가 없으면 같은 자리에서 다음 회차를 바로 시작(현장직원 회차 생성 RLS 016·진행 중 회차 수정 RLS 017, 확정 회차는 수정 불가), 기간 축소로 밖에 남는 출근부·미편입 지출과 회차 겹침은 경고 후 2단계 확인 저장 ②출근부 첨부는 회차당 1부(내부적으로 기간 내 모든 월 시트에 반영 — "붙임: 출근부" 증빙 구조 유지) ③상주 일수는 월별 분할 없이 **기성기간 합계**로 전기(24개월 회차도 동일 UX). 합계는 회차 시작 월 레코드에 저장하고 나머지 월은 0으로 초기화, `work_days` CHECK 31→999 완화(018), 서버가 기성기간 달력 일수 초과를 검증 ④기술지원은 기간 전체 방문일 칩 입력(월 경계 없음) |
| 2026-08-10 | F-11  | **출근부 PDF 자동 인식** — 첨부 선택 즉시 PDF 텍스트 레이어에서(`unpdf`, Y좌표 행 복원) 명부 인원별 출근일수·방문일을 추출해 입력란 자동 채움(제안값, 확인 후 저장). 통합본 총괄표("2026-04 22일 22일")·월별 "근무일수 합계"·단일 월 문서 모두 인식, 상주는 월별 인식값을 합산해 합계로 주입. 스캔 이미지(텍스트 없음)는 안내 후 수기 입력 폴백. 실제 테스트 출근부 PDF 픽스처 기반 vitest 5종 |
| 2026-08-10 | F-16  | **주재비·출장비 폼 출근부 연동 강화** — 근무일수가 진행 중 회차의 출근부 출근일수 합계로 프리필(조회 월 무관), 근무기간 기본값=기성기간. 근무기간 변경 시 근무일수를 달력 일수로 덮어쓰던 동작 제거(휴가·결근 반영된 출근부가 원천, 출근부 기록 없는 추가 인원만 자동 계산 유지). 기술지원 출장비 방문일도 회차 전체 기준 프리필 |
| 2026-08-10 | 계상모델 | **기성회차 기간 입력 개선** — 회차 생성·수정 기간을 연·월·일 날짜(`type=date`)로 입력(예본 1회차처럼 월 중간 시작 지원, 연도 경계 회차 허용), 용어를 "기성기간"으로 통일. 현장직원 기성회차 화면에도 새 회차 시작 폼 추가(증빙 첨부 주체가 현장이므로 — 확정은 본사 전용 유지). zod 스키마 테스트 6종 |
| 2026-08-10 | 테스트환경 | **5회차 검증 데이터 정비** — 샌드박스 "도매시장 5회차" 5회차 기성기간을 2026-04-01~08-31로 변경, 1~4회차를 계약 시작(2025-06-01) 이후 연속 기간으로 정리(4회차 2025-12-01~2026-03-31, 예본과 동일). 테스트 증빙 재생성: 출근부 통합본 2부(상주 106일/인·기술지원 방문 10일/인, 2026-05-05·05-25·08-17 공휴일 반영) + 월별 영수증 80종(숙소·관리비 20, 방문일별 출장 유류·통행료 40, 현장경비 20 — 복리후생 월 120,000 vs 한도 100,000 삭감 시나리오 5개월 유지), `Desktop\테스트영수증_5회차\` 연월 폴더 구조 |
| 2026-08-04 | F-15  | **오피넷 유가 자동입력 연동** — 예본 각주("유가 등은 운행일자의 오피넷 고시 유가를 적용") 근거로 한국석유공사 유가정보 API 연동(무료 Key, 일 1,500건). ①`opinet.ts`(전국 일별 평균가, 유종 매핑: 휘발유·하이브리드·PHEV→B027/경유→D047/LPG→K015, 전기·수소 미지원) ②`fuel_prices` 캐시 테이블(015) — 무료 API가 최근 7일 중심이라 조회분을 날짜별 적재해 과거 운행일 재사용 ③자차 산출 패널 유가 "자동" 버튼 + 출장비 방문일 선택 시 해당일 유가 자동 채움(수기 덮어쓰기 허용). 실호출 검증: 2026-08-04 휘발유 전국 평균 1,866원 |
| 2026-08-04 | F-15  | **카카오 API 전환·활성화** — 미작동이던 NCP Maps 연동(`naverMaps.ts`)을 카카오로 교체(`kakaoMaps.ts`): 카카오 로컬 지오코딩 + 카카오모빌리티 자동차 길찾기(무료 일 10,000건, 초과 8원/건). developers.kakao.com `CM직접경비정산` 앱 REST 키 발급, 카카오맵 사용 설정 ON(무료 쿼터 첫 앱 영구 귀속 동의), `.env.local`에 `KAKAO_REST_API_KEY` 설정. 실호출 검증 완료. 자차 산출 패널의 "자동" 버튼이 이제 실제로 거리·통행료를 채움 |
| 2026-08-04 | 테스트환경 | **5회차 실영수증 검증 준비** — 예본 1~5회 산술 검증 완료(누계 179,810,583 = 계상총액, 주재비 +7,723,343 초과를 출장비·인쇄비 미달로 정확히 상쇄, 5회차 미지급 22,070,912 확인). 샌드박스 "도매시장 5회차" 명부를 상주 2명(강희철 책임/성혁기 건축)+기술지원 2명(류익선 건축/장재근 토목)으로 확정, 5회차 예본 월단가 기반 테스트 영수증 PDF 14종 생성(Desktop\테스트영수증_5회차 — 워터마크·가상거래 고지 포함, 식대 기대값 575,000/인·복리후생 한도 100,000 vs 증빙 120,000 삭감 시나리오) |
| 2026-08-04 | 인원모델 | **정산 인원 = 기술인 명부 단일 원천 확정** — 출근부·주재비·출장비 화면의 인원 목록에서 로그인 계정(현장 배정) 자동 표시를 제거. 계정은 로그인·권한용으로만 쓰고, 정산 대상 기술인은 명부(site_staff_members)에서만 온다(계정 사용자가 정산 대상이면 명부에 이름 추가). 사용자 결정: 테스트 명단은 상주 4명(강희철 책임/성혁기 건축/이준일 토목/지창호 기계) + 기술지원 2명(류익선 건축/장재근 토목), 예본 전체 명단 12명은 비활성 보존 |
| 2026-08-03 | F-11  | **주재비·출장비 폼 명부 인원 연동** — 상주기술인 주재비 폼과 기술지원 출장비 폼의 인원 표에 계정 인원 + 명부 인원(`site_staff_members`)이 함께 표시. 명부 인원은 이름으로 식별(기존 추가 행과 동일한 target_user_name 방식 — 서버 액션 변경 없음). 출근부 일수(상주, member_id 기준)와 방문일자(기술지원)가 폼에 자동 프리필: 출근부에 방문일을 입력해두면 출장비 방문일 행이 미리 생성됨. 직종도 명부값 자동 적용 |
| 2026-08-03 | F-11  | **현장 기술인 명부 (`site_staff_members`, migration 014)** — 정산 대상 기술인은 대부분 로그인 계정이 없으므로(예본: 상주 13명·기술지원 5명) 출근부 화면에서 "+ 인원 추가"로 명부에 등록(성명·직종). 상주/기술지원 두 섹션 모두 명단 표 + 인원 추가·제외(비활성, 기록 보존) 지원. `attendance_records`가 계정(user_id) 또는 명부(member_id)를 택일 참조하도록 확장(CHECK + 명부용 유니크). 검증: 류익선(건축) 추가→방문일 2026-08-05 저장 확인 |
| 2026-08-03 | F-11  | **출근부 첨부 모델 (상주/기술지원 분리)** — 정산서 예본 분석 결과 출근부는 식대(횟수×단가)·출퇴근교통비(왕복비×근무일수)·출장비(방문일 입증)의 산출 근거 증빙("붙임: 출근부 1부"). 실무 흐름대로 **현장이 작성·서명한 출근부를 첨부(업로드)하고 기재 일수를 전기**하는 구조로 확정 — 시스템이 출근부를 생성하지 않음. ①`attendance_sheets`(현장×연월×구분 첨부, migration 013) + `attendance_records.visit_dates`(기술지원 방문일) ②출근부 화면 개편: 상주기술인(일수 입력)/기술지원 기술인(방문일자 칩 입력) 섹션 분리, 각각 별도 첨부, 미첨부 경고 ③일수는 기존대로 주재비 폼에 자동 주입. 2단계(출장비 방문일 대조 검증·식대 자동산출 강화)는 후속 |
| 2026-08-03 | F-22  | **정산서 PDF 출력 구현** — 엑셀 정산서와 동일 서식(총괄 계약내용/직접경비 사용금액/항목별 사용금액 3단 트리 + 세부 사용내역 섹션 전체: 숙소비·식대·교통비(자차 산출 포함)·건별 실비·복리후생비·출장비(방문일 상세 포함))을 pdfmake로 재현. 한글 폰트는 Noto Sans KR(OFL, npm `@expo-google-fonts/noto-sans-kr` TTF) 임베딩, 숙소비·교통비·출장비 섹션은 가로 방향 페이지. `/api/reports/pdf` Route Handler(엑셀 라우트와 동일 권한 기준) + 기성회차 화면(현장직원 `/settlement`·본사 `/admin/sites/[siteId]/settlement`) 확정 회차·잠정 정산서 PDF 다운로드 버튼. 섹션 번호 로직은 `buildSectionNumbers`로 엑셀·PDF 공유, 스모크 테스트 2종(`pdf.test.ts`) + `vitest.config.ts` 별칭 추가. F-23(불인정분 합계 제외·미지급 경고 각주)도 동일 반영 |
| 2026-07-31 | 개발환경 | **로컬 dev 실행 설정 기기 독립화** — `launch.json`의 dev 서버 실행 경로를 특정 PC 절대경로에서 프로젝트 상대경로(`.claude\dev-wrapper.cmd`)로 변경, wrapper는 winget node 경로가 존재하는 PC에서만 PATH에 추가하도록 조건 처리(주석 영문화로 cmd 인코딩 오류 제거). 두 PC(사무실/자택) 어디서든 ▷ 실행 가능 |
| 2026-07-30 | UX원칙 | **"채움" 관점 색 체계 확립** — 직접경비는 발주청이 증빙 확인 후 지급하므로(직접인건비·제경비·기술료는 전액 지급) 계상금액은 "아껴 쓰는 예산"이 아니라 "증빙으로 채워야 전액 받는 목표"다. 전 화면 색 통일: **파랑=충족**(계상 대비 100%↑, 항목 초과분은 총액 내 흡수·정상 지급), **빨강=미충당**(증빙 부족분 = 예상 삭감액, 금액 병기), 주황/빨강=총액 초과 사용(청구 불가 미지급). 기성회차(현장·본사) 표에 채움률 % 열 추가, 대시보드 위젯을 "계상 채움 현황"으로 개편(충족 ✓ / 미충당 X원), 경고 문구를 "삭감 후 지급 예정" 등 실지급 관점으로 재작성 |
| 2026-07-30 | 계상모델 | **확정 회차 이력 표 강화 + 실데이터 시딩** — 현장직원 기성회차 화면의 확정 이력 표를 정산서 2번 표 구조로 확장(회차/기간/금회계상/금회사용/금회기성/누계기성/잔액 + 미지급·계상부족 표시), 진행 중 회차 카드에 금회 계상금액 표시, 본사 화면에도 금회계상 열 추가. 청주 농수산물 도매시장 현장을 실제 정산서 기준으로 시딩(계상 179,810,583 = 주재비 164,929,334/출장비 12,693,879/도서인쇄 2,187,370, 1~4회차 확정 스냅샷 + 5회차 진행 중, 계상 5,513,304). 유의: 1~2회차는 계약변경 전 총액(179,719,060) 기준이라 예본 잔액과 91,523원 차이 — 시스템은 현행 계약총액 단일 관리 |
| 2026-07-30 | F-17/18 | **개별 승인·반려 워크플로 구현** — `/hq/review` 제출 검토 화면(본사담당자·관리자 전용, `/hq/*` 미들웨어 가드 추가): 현장별 그룹 목록 + 영수증 확인 + 개별/일괄 승인 + 반려(사유 필수, 현장 직원 화면에 표시). 승인=`approved`(회차 미편입 유지), 반려=`rejected`(회차 편입 제외, 현장은 삭제 후 재입력). 확정 RPC v3(012)가 `submitted`+`approved` 미편입분을 함께 편입, `reviewed_by/reviewed_at` 감사 컬럼 추가. 집계 쿼리 5곳(대시보드·기성회차 2곳·budgetStatus·정산서)에 approved 미편입분 포함 |
| 2026-07-29 | 계상모델 | **현장직원 계상금액 입력 허용** — 기성회차 현황(`/settlement`)에 항목별 계상금액 입력 카드(접이식) 추가. 계약 정보를 아는 현장 담당자가 직접 기재해야 사용액·잔액 추적이 시작되는 실무 반영. 쓰기는 `upsert_site_expense_budgets` RPC(011)로만 — 현장 소속원·본사·관리자 허용 + 계상총액 자동 동기화 |
| 2026-07-29 | F-08/F-12 | **비용 입력 폼 잔여 계상액 실시간 안내** — 비목 선택 버튼에 항목별 계상 잔액(초과 시 빨강) 표시, 금액 입력 시 ①항목 잔액 초과→총액 내 흡수 가능 안내(주황) ②총액 잔액 초과→초과분 청구 불가·미지급 위험 경고(빨강, 저장은 허용). VAT 제외 적용금액 기준. `getSiteBudgetStatus` 헬퍼 신설 |
| 2026-07-29 | F-10  | **대시보드 항목별 계상 잔액 위젯** — 계약 전체 기준 항목별 계상금액 대비 누계 사용(확정 회차 청구액 + 미편입 인정액, 현장 전체) 진행바·잔액·초과 표시. 80% 이상 노랑, 초과 빨강. 계상금액 미입력 현장은 미노출 폴백 |
| 2026-07-29 | 계상모델 | **현장직원 화면에 계상 대비 현황 노출** — 기성회차 현황(`/settlement`)에 읽기 전용 [항목별 계약금액/누계기성/(금회사용/금회기성)/잔액] 표 + 경고 3종(잔액 초과 미지급 / 항목 초과 총액 내 흡수 / 금회 계상 대비 부족→증빙 보완 안내). 진행 중 회차가 없으면 금회 열 자동 생략. 삭감 위험을 현장이 먼저 인지하도록 하는 목적 |
| 2026-07-29 | 계상모델 | **항목별 계상금액·청구 캡 모델 구현 (§3.3 신설)** — 차수별 정산서 예본(청주 도매시장 1~5회 기성) 분석 반영. ①`site_expense_budgets`(현장×비목 계상금액, 현장 폼에서 입력·총액 자동합산) ②`settlement_rounds.budgeted_amount/claim_amount` + `settlement_round_items`(회차×항목 [계약금액/전회누계/사용액/청구액] 스냅샷) ③확정 RPC가 청구액=min(사용액, 계상총액 잔액) 계산, 항목별 비례 배분 ④`calcClaim` 순수 함수(골든 테스트 5종: 5회차 캡 5,513,304 재현) ⑤기성회차 화면에 항목별 계상 대비 표 + 미지급·항목초과(총액 내 흡수)·계상부족(삭감 위험) 경고 ⑥엑셀 2번 표 항목별 계약금액/전회누계/금회기성/잔액 + 고시 제2023-580호 각주 |
| 2026-07-29 | UI    | 사이드바 메뉴 명칭 정정 — 기술지원 인원은 주재비가 아닌 출장비로 정산하므로 "기술지원 주재비→기술지원 출장비", "직접경비 입력→현장경비 입력", "기성회차→기성회차 정산" (정산서 3갈래 구분: 주재비/출장비/현장경비와 일치) |
| 2026-07-29 | F-21/23 | **정산서 엑셀(xlsx) 출력 구현** — 실제 수작업 정산서 서식(총괄표 계약내용/직접경비 사용금액/항목별 3단 트리 + 증빙서류 열 + 세부섹션 번호 + 서명란, 세부 시트 1-1 숙소비/1-2 식대/1-3 교통비/현장운영경비/복리후생/2-1 출장비) 재현. `exceljs` 사용, `/api/reports/excel` Route Handler + 기성회차 화면 다운로드 버튼. 인정금액(=amount-불인정분) 기준 집계로 F-23 반영. PDF(F-22)·증빙묶음(F-24)은 후속 |
| 2026-07-29 | 계산모델 | **실제 정산 규정 반영 계산 엔진 신설** (`src/lib/settlement/`, vitest 골든 테스트 22종): ①VAT제외 적용금액(`round(gross/1.1)`) ②복리후생비 월별 `min(인원×한도, 증빙)` 정산 ③상주기술인 교통비 이원 모델(숙박형=왕복비×월횟수 / 출퇴근형=왕복비×근무일수) ④기술지원 기술인 출장비 방문일별 산출(유류비+통행료+일비+식비). 클라이언트는 미리보기, 서버가 동일 함수로 재계산해 확정 |
| 2026-07-29 | 스키마 | 상세 모델 마이그레이션(008 유실 테이블 복원 baseline, 009 additive) — `expense_items`(건별 내역), `commute_calcs`, `trip_visits`, `welfare_settlements`, `company_profile` + `expenses`에 `amount_gross`/`vat_mode`/`period`/`specialty`/`calc_detail` 컬럼, `site_parameters`에 일비·식비·월횟수 파라미터. `expense_date`를 저장일→정산월 말일로 수정(기성회차 편입 누락 방지) |
| 2026-07-23 | 버그 수정 | `expenses` RLS 정책이 앱이 쓰지 않는 컬럼(`submission_status`)을 참조해 본사 담당자가 제출 항목을 조회할 수 없던 문제 수정 (F-17/18/20의 전제조건) |
| 2026-07-23 | F-20  | 본사 전체 현황 화면(`/hq/overview`) 구현 — 전 현장 예산·확정사용액·잔액·검토대기 일람. 승인·반려 개별 처리(F-17/18)는 미포함, 별도 과제               |
| 2026-07-23 | F-16  | 인원별 주재비 화면의 식대 한도·출퇴근교통비 적용여부를 현장별 `site_parameters`와 연동 (기존엔 25,000원 하드코딩)                               |
| 2026-07-23 | F-14  | 통신비 입력 시 "개인 휴대폰 요금 아님" 확인 체크 전까지 저장 차단                                                                   |
| 2026-07-23 | 추가    | 기성회차 삭감 위험 경고 문구(잔액 남을 시), 현장직원 화면에 기성회차 현황 읽기 전용 노출 추가                                                   |
| 2026-07-23 | F-15  | 자차 교통비 자동산출 구현 — `profiles.home_address`/`vehicle_fuel_type`, `sites.address` 추가, NCP Maps API로 거리·통행료 조회 후 「공무원보수 등의 업무지침」 공식으로 연료비 계산. NCP API 키 발급 전까지는 미작동(별도 과제) |
| 2026-07-23 | F-04  | 현장 배정에 상주기술인/기술지원 구분(`staff_type`) 추가, 인원별 주재비 입력 화면을 두 화면으로 분리                                              |

---

## 9. 용어 정의 (Glossary)

| 용어          | 정의                                                                |
| ----------- | ----------------------------------------------------------------- |
| 직접경비        | 건설사업관리 용역 수행에 직접 소요되는 비용 (현장주재비, 차량운행비, 출장비 등). 간접비·이윤과 구분됨       |
| 기성          | 계약된 용역의 수행 실적에 따라 발주처에 청구하는 대가. 월별 또는 공정 단계별로 청구                  |
| 실비 적용       | 실제 지출한 금액을 영수증 증빙 기반으로 정산하는 방식 (한도 없음, 단 목적 부합 필수)                |
| 공무원 여비규정    | 출장비, 교통비, 일비 등의 한도 및 산정 기준을 규정한 법령. 건설엔지니어링 직접경비의 교통비·출장비 기준으로 준용 |
| 불인정         | 정산 기준에 부합하지 않아 발주청이 해당 금액을 정산 대상에서 제외하는 것                         |
| CM (건설사업관리) | Construction Management. 발주청을 대리하여 공사 전반의 기획·설계·시공·유지관리를 관리하는 용역  |
| MVP         | Minimum Viable Product. 핵심 기능만으로 구성된 최초 출시 버전                     |
| RBAC        | Role-Based Access Control. 사용자 역할에 따라 시스템 접근 권한을 차등 부여하는 보안 방식    |

---

*선엔지니어링종합건축사사무소 건설사업관리본부 | 내부 배포용 문서*
